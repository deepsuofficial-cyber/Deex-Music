import {useCallback,useEffect,useState} from 'react';import {Storage} from '../storage/Storage';
export function useLibrary(){const [favorites,setFavorites]=useState<string[]>([]);const [recent,setRecent]=useState<string[]>([]);const [playlists,setPlaylists]=useState<any[]>([]);const refresh=useCallback(async()=>{setFavorites(await Storage.getFavorites());setRecent(await Storage.getRecent());setPlaylists(await Storage.getPlaylists());},[]);useEffect(()=>{refresh();},[refresh]);const toggleFavorite=async(id:string)=>{const next=favorites.includes(id)?favorites.filter(x=>x!==id):[...favorites,id];setFavorites(next);await Storage.setFavorites(next);};return {favorites,recent,playlists,refresh,toggleFavorite};}
